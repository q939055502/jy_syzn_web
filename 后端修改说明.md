# 后端修改说明

## 一、委托单下载链接优化

### 1. 背景与目标
- **背景**：原委托单下载链接为永久链接，存在安全隐患，且链接生成逻辑分散在多处
- **目标**：实现带签名和过期时间的临时下载链接，集中管理链接生成逻辑，增强安全性

### 2. 技术方案
- 实现基于 HMAC-SHA256 算法的签名机制
- 生成带过期时间的临时下载链接
- 集中化管理链接生成和验证逻辑
- 支持 IP 限制和链接失效机制

### 3. 修改内容

#### 3.1 创建链接生成服务
- **文件**：`app/services/utils/link_generator.py`
- **功能**：
  - `generate_signed_url`：生成带签名和过期时间的下载链接
  - `validate_signed_url`：验证链接签名和有效性
  - 支持自定义过期时间（默认 3600 秒）
  - 可选 IP 限制功能

#### 3.2 更新文件下载路由
- **文件**：`app/routes/public/files.py`
- **功能**：
  - 移除了废弃的 `download_file` 接口
  - 添加了 `download_signed_file` 接口，处理带签名的下载请求
  - 增强了安全验证，防止未授权访问
  - 统一了文件响应生成逻辑

#### 3.3 统一链接生成逻辑
- **委托单模板服务**：`delegation_form_template_service.py`
  - 修改 `_generate_download_url` 方法，使用新的链接生成服务
- **公共检测路由**：`public/detection.py`
  - 更新模板下载链接生成逻辑
- **检测参数模型**：`detection_param.py`
  - 修改 `to_dict` 方法中的链接生成逻辑

#### 3.4 添加配置项
- **文件**：`.env`
- **新增配置**：
  - `SIGNING_KEY`：用于生成签名的密钥
  - `DEFAULT_DOWNLOAD_LINK_EXPIRE`：默认链接过期时间（3600 秒）

### 4. 优化效果

#### 4.1 安全性增强
- 生成的链接带有签名和过期时间，防止未授权访问
- 支持 IP 限制，进一步增强安全性
- 链接过期后自动失效，减少安全风险

#### 4.2 集中化管理
- 统一的链接生成逻辑，便于维护和扩展
- 集中的配置管理，支持灵活调整

#### 4.3 统一格式
- 所有下载链接使用相同的格式
- 一致的签名验证机制

### 5. 对前端的影响

#### 5.1 链接格式变化
- 原链接格式：`/api/public/files/download/{file_path}`
- 新链接格式：`/api/public/files/download/signed?file_path={file_path}&expire={timestamp}&signature={signature}`

#### 5.2 前端适配要求
1. **无需额外开发**：前端无需修改任何代码，直接使用返回的 `template_download_url` 即可
2. **链接使用**：生成的链接包含完整的 URL，前端可直接用于下载
3. **错误处理**：添加对 403 状态码的处理，提示用户链接无效或已过期
4. **链接刷新**：如果链接过期，需重新请求获取新的下载链接

## 二、其他修改

### 1. 移除废弃接口
- 删除了 `app/routes/public/files.py` 中的 `download_file` 接口
- 该接口已被标记为废弃，不再建议使用

### 2. 代码优化
- 提取了共用的文件响应生成逻辑
- 增强了错误处理和日志记录
- 优化了代码结构，提高了可读性和可维护性

## 三、版本兼容性

- **向后兼容**：所有修改保持了向后兼容，不会影响现有功能
- **平滑过渡**：前端无需修改即可使用新的链接生成机制
- **安全性增强**：所有下载链接自动生成带签名的临时链接

## 四、部署注意事项

1. **配置更新**：确保在生产环境中更新 `SIGNING_KEY` 为强密钥
2. **密钥管理**：妥善保管签名密钥，避免泄露
3. **过期时间设置**：根据实际需求调整 `DEFAULT_DOWNLOAD_LINK_EXPIRE` 配置
4. **监控**：建议添加链接使用情况的监控和日志记录

## 五、测试建议

1. **功能测试**：验证下载链接的生成和使用
2. **安全性测试**：测试无效签名、过期链接的处理
3. **性能测试**：测试高并发下的链接生成和验证性能
4. **兼容性测试**：确保与现有前端代码的兼容性

以上修改已完成，前端可以根据 API 接口说明文档进行适配更新。