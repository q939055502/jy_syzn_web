根据用户要求，我需要将"角色权限"拆分为"角色管理"和"权限管理"两个选项，具体修改如下：

### 1. 修改左侧菜单
- 在桌面端侧边栏菜单中，将"角色权限"替换为"角色管理"和"权限管理"两个选项
- 在移动端抽屉菜单中，同样将"角色权限"替换为"角色管理"和"权限管理"
- 保持与其他菜单项一致的图标和样式
- 确保两个新选项位于"用户管理"和"系统设置"之间

### 2. 创建视图组件
- 创建 `src/views/RoleManagement.vue` 视图组件
- 创建 `src/views/PermissionManagement.vue` 视图组件
- 添加基本的页面结构和样式
- 为后续开发预留逻辑代码位置

### 3. 更新路由配置
- 删除原来的 `RolePermission` 导入
- 添加 `RoleManagement` 和 `PermissionManagement` 的导入
- 删除原来的 `/role-permission` 路由
- 添加 `/role-management` 和 `/permission-management` 两个新路由
- 确保路由顺序与菜单顺序一致

### 4. 清理旧文件
- 删除不再使用的 `src/views/RolePermission.vue` 文件

### 5. 验证修改
- 运行诊断工具，确保没有语法错误
- 确保菜单选项与路由配置正确对应
- 保持代码风格的一致性

这个修改将使左侧菜单更加清晰地展示功能模块，便于用户区分和使用角色管理与权限管理功能。