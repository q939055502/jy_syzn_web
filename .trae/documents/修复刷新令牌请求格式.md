根据更新后的API文档和错误信息，我需要修正刷新令牌请求的传参方式。以下是修复计划：

### 问题分析
- 后端错误信息显示：`Input should be a valid dictionary or object to extract files from`
- 更新后的API文档明确要求：请求体必须是包含 `refresh_token` 字段的JSON对象
- 当前实现错误地直接发送刷新令牌字符串，而不是JSON对象

### 修复方案
1. **修正 `src/api/user.js` 中的 `refreshToken` 函数**
   - 将请求体从直接发送字符串改为发送JSON对象 `{ refresh_token: refreshToken }`
   - 确保设置正确的 `Content-Type: application/json` 头

2. **修正 `src/api/interceptors/response.js` 中的刷新令牌请求**
   - 将请求体从直接发送字符串改为发送JSON对象 `{ refresh_token: refreshToken }`
   - 确保设置正确的 `Content-Type: application/json` 头

3. **保持请求拦截器的现有逻辑**
   - 请求拦截器已正确配置为不为刷新令牌请求添加Authorization头
   - 日志打印逻辑已兼容多种请求格式，无需修改

### 修复后的请求格式
- **请求URL**：`POST /api/auth/refresh`
- **请求头**：`Content-Type: application/json`
- **请求体**：`{ "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." }`

### 预期效果
- 刷新令牌请求将符合后端API的预期格式
- 不再出现422错误
- 成功实现令牌轮转机制
- 刷新页面时保持登录状态

### 验证方式
- 通过开发服务器测试
- 检查浏览器控制台日志
- 验证刷新令牌请求是否返回200状态码
- 验证是否成功获取新的访问令牌和刷新令牌