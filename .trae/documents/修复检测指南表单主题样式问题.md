1. **添加选中标签样式**：在DetectionGuideForm.vue中添加选中标签的固定背景色样式，确保在深色主题下有良好的对比度
2. **修改位置**：在src/views/form/DetectionGuideForm.vue的样式部分添加以下CSS：

   ```css
   :deep(.el-select__tags .el-tag) {
     background-color: #2c3e50 !important;
     color: #ffffff !important;
     border-color: #34495e !important;
   }
   ```
3. **确保样式生效**：该样式将确保选中的检测规范标签在深色主题下有固定的深色背景和白色文字，解决对比度问题

