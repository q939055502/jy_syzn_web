根据用户要求，我需要在送样指南下面增加一个现场指南选项，具体修改如下：

### 1. 修改左侧菜单
- **桌面端侧边栏菜单**：在送样指南下面添加现场指南选项
- **移动端抽屉菜单**：在送样指南下面添加现场指南选项
- 使用与送样指南相同的图标和样式，保持一致性

### 2. 创建视图组件
- 创建 `src/views/FieldGuide.vue` 视图组件
- 添加基本的页面结构和样式
- 为后续开发预留逻辑代码位置

### 3. 更新路由配置
- 在 `src/router/index.js` 中导入新创建的组件
- 添加 `/field-guide` 路由，放在 `/sample-guide` 路由之后
- 设置正确的 meta 信息，包括标题

### 4. 保持代码风格一致
- 遵循项目现有的代码结构和命名规范
- 保持与其他菜单项相同的样式
- 确保路由配置的顺序与菜单顺序一致

### 5. 预期效果
- 桌面端和移动端菜单中都能看到现场指南选项
- 点击现场指南选项能跳转到对应的页面
- 页面具有基本的结构和样式
- 代码风格保持一致，易于维护

### 6. 验证方式
- 启动开发服务器
- 检查左侧菜单是否显示现场指南选项
- 点击选项，检查是否能正确跳转到现场指南页面
- 确保页面样式正确

这个修改将为用户提供一个新的现场指南选项，方便用户查看相关内容，同时保持了代码的一致性和可维护性。