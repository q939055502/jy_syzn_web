## 重构计划：DelegationFormTemplateUsage.vue 组件

### 1. 布局修改
- 将当前的卡片式布局改为表格布局
- 使用 Element Plus 的 `el-table` 组件替代卡片列表

### 2. 表头设计
- 简化表头为三列：检测对象、检测项目、检测参数
- 移除复杂的状态标签，仅通过字体颜色区分启用/禁用状态

### 3. 状态显示
- 启用状态：默认文本颜色
- 禁用状态：红色文本（#ff4949）
- 移除当前的状态标签和复杂样式

### 4. 核心功能保留
- 保留加载状态显示
- 保留空状态显示
- 保留主题支持
- 保留相同的 API 集成和数据处理逻辑

### 5. 代码结构优化
- 使用计算属性处理数据格式转换
- 简化模板结构，提高可读性
- 保留必要的样式，移除冗余的深度样式修改

### 6. 具体实现步骤
1. 修改模板结构，替换卡片列表为表格
2. 简化数据处理逻辑，使用计算属性转换数据格式
3. 添加禁用状态的文本样式
4. 优化表格样式，适配主题
5. 保留原有功能逻辑（加载、空状态、事件处理）

### 7. 预期效果
- 简洁的表格布局，易于浏览
- 清晰的状态区分，禁用项显示为红色文本
- 保持与其他组件一致的主题风格
- 提高页面加载和渲染性能