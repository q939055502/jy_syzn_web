## 完善检测规范删除功能

### 1. 功能分析
目前删除功能已经基本实现，但存在以下问题：
- 批量删除功能被注释掉了（v-if="false"）
- 需要确保删除功能符合API文档要求
- 检查权限控制和错误处理

### 2. 实现计划

#### 2.1 启用批量删除功能
- 移除批量删除按钮的 `v-if="false"` 条件
- 确保批量删除功能正常工作

#### 2.2 优化删除确认对话框
- 增强删除确认信息，显示删除的检测规范名称
- 区分单个删除和批量删除的确认信息

#### 2.3 完善错误处理
- 确保所有可能的错误情况都被处理
- 提供清晰的错误提示给用户

#### 2.4 检查权限控制
- 确保只有管理员可以删除检测规范
- 添加权限检查逻辑

#### 2.5 测试删除功能
- 测试单个删除功能
- 测试批量删除功能
- 测试各种错误情况

### 3. 技术实现

#### 3.1 前端组件修改
- `DetectionStandard.vue`：启用批量删除按钮，优化确认对话框

#### 3.2 服务层和API层
- 确保 `deleteDetectionStandard` 方法正确处理API响应
- 检查HTTP状态码处理，特别是204状态码

#### 3.3 权限控制
- 添加权限检查逻辑，确保只有管理员可以删除检测规范

### 4. 预期结果
- 单个删除功能正常工作
- 批量删除功能正常工作
- 删除前显示明确的确认信息
- 错误情况下显示清晰的错误提示
- 只有管理员可以删除检测规范

### 5. 风险评估
- 权限控制可能需要后端支持
- 批量删除操作需要谨慎处理，避免误操作
- 需要确保删除操作不会影响其他相关数据

### 6. 测试计划
- 测试单个删除功能
- 测试批量删除功能
- 测试删除不存在的检测规范
- 测试无权限删除的情况
- 测试网络错误情况

### 7. 代码规范
- 遵循项目现有的代码风格和命名规范
- 添加适当的注释
- 确保代码的可维护性和可扩展性

### 8. 依赖检查
- 确保所有依赖都已正确安装
- 检查Axios配置是否正确
- 确保API接口符合文档要求