To fix the date picker to only show year-month-day selection, I need to update the DetectionStandardForm.vue file:

1. **Change date picker type**: Update both `effective_time` and `invalid_time` el-date-picker components from `type="datetime"` to `type="date"`
2. **Keep existing formatting**: The current formatting in handleSubmit already extracts just the date part, so no changes needed there
3. **Verify functionality**: Ensure the date pickers now only show calendar selection without time input

This change will make the date pickers more user-friendly by only showing what's necessary - year, month, and day - which matches the backend API requirements.