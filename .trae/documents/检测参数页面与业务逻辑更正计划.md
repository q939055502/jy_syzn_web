# 检测参数页面与业务逻辑更正计划

## 一、问题分析

根据后端模型文档（模型说明.md）和API文档（API_DOCUMENTATION.md），检测参数（DetectionParam）模型中没有`unit`和`sample_unit`字段，但前端代码中仍然使用了这两个字段。

## 二、更正内容

### 1. 更新 DetectionParam.vue 组件

- **移除多余字段**：删除表格中的"单位"和"样品单位"列
- **调整表格结构**：更新表格列顺序，确保与后端返回字段一致

### 2. 更新 DetectionParamForm.vue 组件

- **移除多余表单字段**：删除"单位"和"样品单位"输入框
- **调整价格字段处理**：确保价格字段与后端API格式兼容
- **更新表单规则**：移除与删除字段相关的验证规则

### 3. 检查 API 调用和响应处理

- **确认 API 端点**：确保所有 API 调用使用正确的端点（`/detection/params`）
- **检查响应处理**：确保响应中的价格字段（字符串格式，如"100.00元/组"）能正确显示
- **检查请求格式**：确保创建和更新检测参数时，价格字段格式正确（数字格式，如100.0）

## 三、实施步骤

1. **更新 DetectionParam.vue**
   - 删除第83行的"单位"列
   - 删除第86行的"样品单位"列
   - 调整剩余列的顺序和宽度

2. **更新 DetectionParamForm.vue**
   - 删除第64-71行的"单位"输入框
   - 删除第73-80行的"样品单位"输入框
   - 删除 rules 对象中与 unit 和 sample_unit 相关的验证规则
   - 更新 resetForm 函数，移除 unit 和 sample_unit 的重置
   - 更新 handleSubmit 函数，确保价格字段格式正确

3. **测试功能**
   - 运行 `npm run build` 确保没有构建错误
   - 测试检测参数的增删改查功能
   - 验证表格显示和表单提交是否正常

## 四、预期结果

- 前端代码与后端模型完全一致
- 所有检测参数相关功能正常工作
- 构建过程没有错误
- 页面显示与后端返回数据一致

## 五、注意事项

- 确保所有组件和服务中的字段名称与后端模型一致
- 注意价格字段在不同场景下的格式处理
- 保持代码风格与现有代码一致
- 确保所有更改都经过测试验证