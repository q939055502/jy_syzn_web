根据模型说明和API文档，我需要修改送样指南的判定依据展示方式。具体方案如下：

1. **修改DetectionGuideForm.vue**
   - 添加检测规范多选器，替换原有的judge_basis输入框
   - 添加检测规范列表获取逻辑
   - 表单提交时使用standard_ids数组替代judge_basis

2. **修改SamplingGuide.vue**
   - 将表格中的judge_basis字段替换为基于standards关联表的计算属性
   - 实现"规范名称+规范代码"的格式化显示

3. **更新detectionGuideApi.js**
   - 确保API调用支持include_standards参数获取关联的检测规范

4. **更新detectionGuideStore.js**
   - 修改fetchGuideList方法，添加include_standards=true参数
   - 确保检测指南列表包含关联的检测规范信息

5. **调整检测指南服务层**
   - 确保服务层正确处理检测规范关联

修改后，送样指南的判定依据将从关联的检测规范中获取，并以"规范名称+规范代码"的格式展示，符合用户需求。