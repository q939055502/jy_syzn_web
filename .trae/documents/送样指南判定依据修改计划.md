根据API文档和当前代码分析，需要对送样指南进行以下修改：

1. **修改表单组件** (`DetectionGuideForm.vue`)
   - 替换原有的 `judge_basis` 输入框为检测规范多选器
   - 添加检测规范列表获取和选择逻辑
   - 表单提交时使用 `standard_ids` 数组替代 `judge_basis`

2. **更新状态管理** (`detectionGuide.js`)
   - 修改 `fetchGuideList` 方法，添加 `include_standards=true` 参数
   - 确保获取的送样指南包含关联的检测规范信息

3. **修改列表展示** (`SamplingGuide.vue`)
   - 将表格中的 `judge_basis` 字段替换为基于 `standards` 数组的计算属性
   - 实现"规范名称+规范代码"的格式化显示

4. **更新API调用** (`detectionGuideApi.js`)
   - 确保创建和更新方法支持 `standard_ids` 参数

5. **调整服务层** (`detectionGuideService.js`)
   - 确保服务层正确处理检测规范关联

修改后，送样指南的判定依据将从检测规范中获取，并以"规范名称+规范代码"的格式展示，符合用户需求。