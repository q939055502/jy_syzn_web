# 修复检测参数和检测项目相关组件

## 问题分析
后端API已更新，将检测项目组(Detection Project Group)改为检测项目(Detection Item)，将检测项目(Detection Item)改为检测参数(Detection Param)。需要确保前端代码中的所有相关组件都已正确更新。

## 修复计划

### 1. 检查并修复 DetectionItem.vue
- 确认组件中所有变量名、方法名是否正确使用"Item"而非"Group"
- 检查API调用是否指向正确的端点
- 确认组件引用和导入路径是否正确

### 2. 检查并修复 DetectionItemForm.vue
- 确认表单字段是否与检测项目的API结构匹配
- 检查组件中所有变量名、方法名是否正确使用"Item"
- 确认组件引用和导入路径是否正确

### 3. 检查并修复 DetectionParam.vue
- 确认组件中所有变量名、方法名是否正确使用"Param"而非"Item"
- 检查API调用是否指向正确的端点
- 确认组件引用和导入路径是否正确

### 4. 检查并修复 DetectionParamForm.vue
- 确认表单字段是否与检测参数的API结构匹配
- 检查组件中所有变量名、方法名是否正确使用"Param"
- 确认组件引用和导入路径是否正确

### 5. 验证修复结果
- 运行 `npm run build` 确保项目能成功构建
- 检查SamplingGuide页面是否能正常显示数据

## 预期结果
- 所有组件都能正确调用更新后的API端点
- 组件间的引用关系正确
- 项目能成功构建
- SamplingGuide页面能正常显示数据

## 技术细节
- 遵循Vue 3 Composition API和<script setup>语法
- 使用Pinia进行状态管理
- 保持代码风格与现有代码一致
- 确保所有导入路径正确
- 检查所有变量名、方法名的一致性