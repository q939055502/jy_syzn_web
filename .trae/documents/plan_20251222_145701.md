1. **修改标签页的激活状态判断**：将标签页的激活状态从基于`activeTab`变量改为基于当前路由的`fullPath`，确保激活状态与当前路由始终保持一致
2. **监听路由变化**：在标签页组件中添加路由变化监听，当路由变化时，更新标签页的激活状态
3. **简化标签页点击事件处理**：点击标签页时，直接导航到对应的路由，让路由变化监听来更新激活状态
4. **简化侧边栏点击事件处理**：点击侧边栏时，直接导航到对应的路由，让路由变化监听来处理标签页的添加和激活状态更新
5. **确保标签页的添加逻辑正确**：当路由变化时，检查标签页是否已经存在，如果不存在则添加

这些修改将确保侧边栏、标签页和页面加载之间的互动方式与参考网站类似，解决当前标签页激活状态不更新的问题。